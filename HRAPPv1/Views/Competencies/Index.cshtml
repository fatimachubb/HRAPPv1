@model IEnumerable<HRAPPv1.Models.Competency>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CompetencieDescrip)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CompetencieDescrip)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCompetencie">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCompetencie">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCompetencie">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
